import { MutationTree } from 'vuex';
import { InvoicesState } from './state';


const mutation: MutationTree<InvoicesState> = {
    someMutation( /* state: ExampleStateInterface */) {
        // a line to prevent linter errors
    }
}


export default mutation;